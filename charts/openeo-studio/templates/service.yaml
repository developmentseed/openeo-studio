apiVersion: v1
kind: Service
metadata:
  name: {{ include "openeo-studio.fullname" . }}
  labels:
    {{- include "openeo-studio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openeo-studio.selectorLabels" . | nindent 4 }}
